# Public Vite build-time variables only.
# Render is the production source of truth.
# Mirror production values locally in `.env.production` when reproducing a Render build.
VITE_APP_NAME=Emberwake
VITE_APP_ENV=production
VITE_DEFAULT_DEBUG_OVERLAY=false
